const allowedOrigins = [
    'http://localhost:3500',
    'http://localhost:3000'
    // FUTURE: add in other authorized http requests
]

module.exports = allowedOrigins // allowed origins for access to API server